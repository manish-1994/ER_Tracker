// Centralized API configuration for the entire frontend.
// All other modules should import from this file instead of hard‑coding URLs.

import { API_BASE, HEALTH_URL } from "./api";

export { API_BASE, HEALTH_URL };

// Future extensions (e.g., versioned APIs) can be added here.